buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.1'
    }
}
allprojects {
    tasks.withType(Test).configureEach {
     forkEvery = 100
    }
}
